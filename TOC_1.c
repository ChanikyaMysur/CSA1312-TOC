#include <stdio.h>
#include <string.h>

int main() {
    char str[100];

    printf("Enter a string: ");
    scanf("%s", str);

    length = strlen(str);
    if (str[0] == "a" && str[llength-1] == "a") {
            printf("accepted\n");
    } else {
        printf("rejected\n");
    }
}